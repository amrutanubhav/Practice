pipeline {
    agent any
    // environment{
    // }

    stages{
        stage('testing on feature branch') {
            steps {
                sh "env"
                sh "echo test is done on feature branch"
            }
        }
           
    }
}